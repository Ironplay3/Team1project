using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewScript : MonoBehaviour
{
    public int _num = 2;
    public string word;


    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hello world" + _num);

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
